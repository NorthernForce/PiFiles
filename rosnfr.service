[Unit]
Description=rosnfr service with docker compose
PartOf=docker.service
After=docker.service

[Service]
WorkingDirectory=/home/orangepi/PiFiles
ExecStart=/usr/bin/docker-compose up ros2_crabby
ExecStop=/usr/bin/docker-compose down

[Install]
WantedBy=multi-user.target
